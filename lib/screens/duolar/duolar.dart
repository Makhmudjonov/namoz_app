import 'package:flutter/material.dart';

class DuolarPage extends StatelessWidget {
  const DuolarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(
        child: Text("Duolar Page"),
      ),
    );
  }
}
