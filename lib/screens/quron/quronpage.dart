import 'package:flutter/material.dart';

class QuronPage extends StatelessWidget {
  const QuronPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(
        child: Text("Quroon Page"),
      ),
    );
  }
}
