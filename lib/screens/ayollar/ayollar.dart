import 'package:flutter/material.dart';

class AyollarPage extends StatelessWidget {
  const AyollarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(
        child: Text("Ayollar Page"),
      ),
    );
  }
}
